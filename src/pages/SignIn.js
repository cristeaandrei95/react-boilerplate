import React from 'react';
import { FirebaseSignInForm, SignUpLink } from '../components';

export default () => (
    <div>
        <h1>Signin</h1>
        <FirebaseSignInForm />
        <SignUpLink />
    </div>
);